server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/msmx?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: ljh150
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

file:
  upload-dir: ${user.dir}/uploads

app:
  base-url: https://afternoon-next-mural.ngrok-free.dev

deepseek:
  api:
    key: sk-a4a5b07785524ef98a14f3542f1bcbfe
    url: https://api.deepseek.com/v1/chat/completions